export enum OrderStatusEnum {
    Created = "created",
    Pending = "pending",
    Completed = "completed",
    Cancelled = "cancelled",
}

export enum PaymentPublisherEnum {
    Success = "payment:success",
}